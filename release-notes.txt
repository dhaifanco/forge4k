Forge 1.2.1

AI detail enhancement now runs before frame interpolation, reducing unnecessary neural inference. Fixed-clip benchmark: 23.1s to 7.8s; actual export time varies with footage and hardware.
Noise-only exports avoid intermediate PNG sequences. AI stages report frame progress, speed and estimated remaining time.
Update downloads use bounded byte ranges with stall recovery, retries and cancellation. Verified installers remain ready when exports prevent installation.
Fixed invalid-media API failures, duplicate exports, staged-file cleanup, incomplete model detection, false HDR classification and duration formatting.
Existing 1.1/1.2 users whose old updater stalls should install this setup executable once. Data and settings remain outside the application directory.
